<?php

/*

// -----------------  CI/CD PIPELINE PROJECT PART-1 --------------------- //

****** Download Git Guide :-

1). Go to google chrome -> Search 'git download' -> Download stable version -> Click to download
2). Open the downloaded file

preamble --> c:\\programfiles\git -> select components -> select start Menu folder
-> choosing the default editor vim
-> let git decide --> git from the command line and also from the 3rd party

3). Software --> use the openssl library
4). Checkout as-is, commit unix-style line encodings -> use MinTTy -> choose default behaviour
-> get credentials Manager core
-> enable file system caching --> install

5). go to command prompt in laptop

-> git config --global user.name "chetan"
-> git config --global user.email "chetan@gmail.com"
-> git config --global --list

*/